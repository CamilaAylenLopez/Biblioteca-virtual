import React, { useEffect, useState } from 'react';
import { View, Text, FlatList, Image, StyleSheet, ScrollView } from 'react-native';

export default function NuevoPersonaje({ navigation, router }) {
    return (
        <Text>Hola</Text>
    )
}